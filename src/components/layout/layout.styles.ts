import styled from "styled-components"

const DefaultWrapper = styled.div`
	min-height: 100vh;
	display: flex;
`

const MobileWrapper = styled.div`
	min-height: 100vh;
`

const Flex = styled.div`
	display: flex;
	flex-direction: column;
`

export { DefaultWrapper, MobileWrapper, Flex }
